//  memory in javaj  scrip

// stack (primitive dt) , stack (Non Primitive dt)

let username = "arrowstyle"

let tuif = username

tuif = "rick"

// console.log(tuif);

let user = "email"

const name  = "data"

const ino = "56"

// console.log(name + ino);

// console.log(`hello my name is ${name} any my repo count is ${ino}` );

const gameName = new String('responsible for printing the range of ')

// console.log(gameName[0]);
// console.log(gameName.__proto__);

// console.log(gameName.toUpperCase);

console.log(gameName.charAt(1));

const newfile = gameName.substring(1,4);

// console.log(newfile);

// the substring() {funct} is responsible for printing the range of characters from starting index postition value to ending value 
// or depends on user (it only prints the first value )

const newstr = "  arrow   "
console.log(newstr);
console.log(newstr.trim()); // used for triming the spaces in input

const url = "https://google.com/ans454$h"

console.log(url.replace('45','hs'));
console.log(url.includes('da'));

// console.log(gameName.split([' ']));

console.log(url.split('/'));


