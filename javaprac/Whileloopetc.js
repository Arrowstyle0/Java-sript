

let index = 0

while (index < 22) {
    
    // console.log(`value of index is ${index}`);
    index = index + 2
}

let myarr = ["mfoa" , "kaw" , "dawff"]

    let arr = 0

    while (arr < myarr.length) {

        // console.log(`The array has been produced ${myarr}`);

        arr = arr + 1 
    }

score  = 1

do {

    // console.log(`Score is ${score}`);

    score++


} while ( score <= 10);


let table = 0

while (table < 30) 
    {
    table = table
        // console.log(`3 : ${table} `) 
}

for (let index = 0; index <= 10; index++) {
    const element = console.log(`${index} and ${table}`) 
    console.log(element);
}   
